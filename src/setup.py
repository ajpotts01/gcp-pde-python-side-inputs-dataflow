import setuptools

setuptools.setup(
    name="gcp-pde-python-side-inputs-dataflow",
    version="1.0",
    description="MapReduce Beam pipeline dependencies",
    packages=setuptools.find_packages(),
)
